# 自定义光纤参数组合

网页保存的自定义光纤组合会以独立 JSON 文件写入本目录。每个文件包含：

- 光纤物理参数与 V 数；
- 自动检测出的模式数量、模式族、标签及模式求解参数；
- 50 组随机权重/相位光斑的近场、远场裁剪比例；
- P95 × 1.10 安全系数得到的统一固定裁剪半宽比例；
- 网格、FFT 比例、Gamma、输出尺寸等预处理参数。

校准期间生成的临时光场只用于统计，不保存为图片。组合文件可以随项目一起备份。
